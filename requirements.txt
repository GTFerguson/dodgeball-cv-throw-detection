# Direct dependencies of the Python side. Versions are pinned to what the pose
# runs in data/pose/ were produced with: a different detector build places
# different boxes, and the labels record which run was on screen when a box was
# placed. Transitive dependencies are left to pip.
#
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# torch resolves to a CUDA build on Linux. For CPU-only machines install it from
# the CPU index first:
#   .venv/bin/pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu

ultralytics==8.4.128
torch==2.13.0
torchvision==0.28.0
opencv-python==5.0.0.93
numpy==2.5.2
