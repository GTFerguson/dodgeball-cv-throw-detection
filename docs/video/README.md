# Video

The recorded walkthrough's slides and script.

- [script.md](script.md) — beat-by-beat narration with timings, the demo cut-in, recording notes
- `deck.html` — the 10-slide deck, self-contained (images and sounds embedded); built by `build.py` from `deck.src.html` and `assets/`
- `figures/` — the two TikZ figures lifted from `docs/design.tex` (`cascade.tex`, `pipeline.tex`), compiled standalone to PNG for the deck. The pipeline copy adds the rebound stage and says "evidence" where the design figure says "confidence".
